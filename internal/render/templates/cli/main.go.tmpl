package main

import (
	"context"
	"fmt"
	"os"
	"os/signal"

	{{quote (print .Config.Project.Module "/internal/cli")}}
)

func main() {
	ctx, stop := signal.NotifyContext(context.Background(), os.Interrupt)
	err := cli.Run(ctx, os.Args[1:], os.Stdout, nil)
	stop()
	if err != nil {
		fmt.Fprintln(os.Stderr, err)
		os.Exit(1)
	}
}
